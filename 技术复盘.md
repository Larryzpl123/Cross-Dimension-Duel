# 技术复盘：当你的推理很合理，但实测打脸

> 一个 25 角色小球对战模拟器的开发记录。
> 本文不讲"我做了什么功能"，只讲**四次我判断错了、以及是什么让我发现自己错了**。
>
> **Abstract (EN).** A retrospective on building a 25-hero ball-battle simulator. Rather than a feature log, it documents four cases where confident, well-reasoned design decisions turned out to be wrong — a geometry bug masquerading as an AI bug, a balance pass that had silently fitted itself to that bug, a metric that measured the wrong thing, and a subjective judgment that structured reasoning got backwards. The connecting thread is the headless test harness that made each error visible.

---

## 0. 项目与约束

浏览器里的自动对战模拟器：25 个网文/游戏主角各有一套简化技能，小球在竞技场里弹跳、放技能，打到一方全灭。

技术约束是自找的，但影响了后面每个决定：

- **零依赖、零构建**，双击 `index.html` 就能玩
- 纯 Canvas 手写，圆-圆碰撞（球都是圆，不需要物理引擎）
- 后期拆成 `js/engine.js` + `characters/*.js`（一角色一文件）+ `maps/*.js`
- 用传统 `<script src>` 而非 ES modules —— 因为 **ES modules 在 `file://` 下会被 CORS 拦死**，双击直接白屏。为了保住"双击即玩"，放弃了更规范的模块化

真正让这个项目有意思的不是这些，是下面这个东西。

---

## 1. 让复盘成为可能的前提：无头测试台

引擎的 `update(dt)` 和渲染是分开的。所以同一份战斗代码可以：

- **有渲染地跑** → 玩家看到的游戏
- **无渲染地跑** → 后台极速模拟

于是有了强度测试台：25 个角色两两对打，C(25,2)=300 对，每对跑 N 遍，几秒钟出一张完整强度榜 + **克制矩阵**（谁被谁克、百分之多少）。

**关键设计：测试台跑的是真实游戏代码，不是简化模型。** 没有单独写一份"用于分析的战斗逻辑"——那样只会得到"我的模型认为的平衡"，而不是"游戏实际的平衡"。

这一点后来救了我四次。下面每一节的"实测"，都来自这个台子或来自真人试玩。

---

## 2. 第一次打脸：几何问题伪装成 AI 问题

**现象**：AI 对战里，两个近战角色绕着圈就是碰不到对方。用户原话是"二人转"。

**看起来像什么**：AI 决策逻辑写错了。追击目标选错？状态机卡住？

**实际是什么**：一个初中几何问题。

单位以恒定速度移动，每帧最多转向 `ω` 弧度。那么它的**最小转弯半径 = v / ω**。

```
石昊：速度 305 px/s（快 235 × speedMul 1.3），转向 2.4 rad/s
最小转弯半径 = 305 / 2.4 ≈ 127 px
竞技场：400 × 600
```

**他的转弯半径超过了半个场地宽度**。他不是"不想撞"，是物理上转不过那个弯——只能一圈圈绕着敌人跑。实测：赵千夜 vs 石昊，**60 秒内接触帧数 = 1**。

更值得记的是：**这个 bug 有一半是我自己造成的**。更早的时候用户反馈"近战太慢追不上人"，我的修复是给近战加 `speedMul 1.3~1.5`——**只提了速度 v，没动转向速率 ω**。而 r = v/ω，所以我那次"修复"把转弯半径又放大了 30~50%，让问题更严重了。

修复：

```js
// 转向速率必须随速度提高，否则转弯半径大过场地
const TURN_RADIUS = 34;
const turn = Math.max(f.seek, sp / TURN_RADIUS);
```

外加去掉 chase 朝向里那个 0.15 的切向分量——它本来是为了让追击不那么"直愣愣"，实际是绕圈行为的直接来源。

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 赵千夜vs石昊 60秒接触帧 | 1 | **91** |
| 对局超时率 | 14% | **0%** |
| 平均对局时长 | 22.2s | 11.0s |

**教训**：当"AI 行为不对"时，先排查它有没有**物理/几何上的能力**去做那件事，再去查决策逻辑。智能体做不到某件事，可能不是它不想，是它的运动学约束不允许。

**次级教训**：一个参数改动如果只动了公式里的分子，要立刻问自己分母是什么。

---

## 3. 第二次打脸：平衡参数悄悄拟合到了 bug 上

修完移动，我以为只是"近战终于能打中人了"。实际结果：

```
修复前（已调好的平衡）：极差 38pt，25 人里只有 2 个擦边
修复后（一行没改数值）：基石 95%、时宇 93% …… 萧炎 11%、林雷 15%，极差 84pt
```

**为什么**：之前那套平衡里，"放风筝"之所以是个有效战术，**是因为追击方转不过弯**。远程角色的生存能力，一部分来自那个 bug。bug 一修，所有远程单位立刻被追上打死。

也就是说：**我之前调出来的每一个数值，都是对"当时系统行为"的拟合，而那个系统行为里含着一个 bug。** 底层一改，上层参数全部失效。

重做平衡时，最有意思的是发现**有些数值本身就是 bug 的补偿品**：

- 近战的 `speedMul 1.3~1.5` → 本来是补偿"追不上"，现在纯属超模，撤掉
- `CONTACT_CD = 0.45`（撞击伤害冷却）→ 当初定得这么低，是因为撞击**很难命中**；现在命中率高了，必须调到 0.75
- 拉扯 AI 被贴脸时会绕圈跑（切向权重固定）→ 改成距离越近越接近直线逃跑

最终回到极差 38pt，但这次是**建立在正确的移动之上**。

**教训**：修复底层 bug 之后，必须重新验证所有基于旧行为调出来的参数。参数从来不是独立的，它们是对系统当时行为的拟合。
这和机器学习里的道理是一样的：**在错误的模型上拟合出的超参数，模型一换就全废**，而且它们看起来是"调好的"，不会自己报错。

---

## 4. 第三次打脸：指标测的不是我要的东西

测试台跑的是 **AI vs AI**。我和一位朋友玩的是 **人 vs 人**。两边的强度排名**明显不一致**：

> 人类实测："纪宁、陆离、时宇好弱；磐岳、唐三、萧炎好强。"
> 测试台：这几个角色的胜率完全不是这个顺序。

**根因不是数值，是容错率。**

测试台里的 AI：技能一就绪立刻施放、走位始终最优、从不失误。
真人 PvP 里：技能要手按（会忘、会按早），走位靠人（会撞墙、会贪）。

于是：

| 在人手里强 | 为什么 |
|---|---|
| 磐岳 | 护盾是**被动**的，玩家什么都不做也吃满收益 |
| 萧炎 | 火莲追踪强 + 范围大，**闭眼扔也能中** |
| 唐三 | 减速到 35%，对手直接**动不了**，容错拉满 |

| 在人手里弱 | 为什么 |
|---|---|
| 纪宁 | 齐射打空后有真空期，需要**掐时机** |
| 陆离 | 血越少伤害越高，需要**主动冒险** |
| 时宇 | 当时按 J 只是"等召唤"，**玩家毫无参与感** |

修法不是调数字，是改设计：

- 萧炎火莲追踪 2.2 → 1.6 rad/s：**人可以靠走位躲了**，AoE 伤害一点没动。（为此给引擎加了按投射物配置 `homing` 的能力）
- 唐三减速 0.35 → 0.55：被减到三成速度在 PvP 里等于被定身，体验极差
- 时宇**重做**：召唤改成自动，J 键改成"战斗指令"（宠物暴走增伤+加速）。从"按键等待"变成"按键决策"

**代价必须说清楚**：萧炎在 AI 模拟里从 46% 掉到 25%。**两个模式的最优解方向是相反的**——AI 模拟喜欢强追踪（命中率高=强），人类玩家讨厌强追踪（躲不掉=不公平）。

**教训**：一个指标优化到极致 ≠ 目标达成。要经常回头问"**我测的这个东西，是不是我真正想要的东西**"。
这里的答案是：测试台衡量的是"机制强度"，而 PvP 需要的是"人类可操作性"。两者相关但不等价，冲突时**以真人体验为准**，测试台退化为"防止某个角色在模拟里彻底崩掉"的护栏。

---

## 5. 第四次打脸：主观体验不能靠结构化推理

做锦标赛模式时，我实现了三种配对算法，并**默认推荐了"强弱分区"**（最强碰最弱，像真实体育赛事的种子制）。

我的理由听起来很像回事：

> 如果每场都强行 50/50，冠军就变成 24 次抛硬币的结果，"谁最强"的叙事就没了。
> 种子制让顶尖选手晚相遇，**决赛才是最强对决**，一期视频有渐入高潮的结构。

用户实测后的反馈：**"都是势均力敌最好看，强弱分区最不好看。"**

我的推理错在哪：

1. "渐入高潮"依赖**长赛程 + 解说 + 背景故事**去撑住前期的碾压。这里**一场只有 10 秒**，每一场都得自己抓住注意力，没有"忍一下后面会精彩"这回事。
2. 当前 25 人强度极差只有 38pt——种子制的首轮既不悬念，**也不够碾压到有观赏性**，两头不靠。

数据上三种模式确实做出了区分（首轮配对偏离 50% 的平均幅度）：

| 模式 | 平均偏离 | 实测观感 |
|---|---|---|
| 势均力敌 | **4.2pt** | 最好看（已设为默认） |
| 纯随机 | 22.9pt | 中间 |
| 强弱分区 | 27.1pt | 最差 |

算法本身没问题，**错的是我对"什么好看"的判断**。

**教训**：可测量的东西（胜率、偏离度）可以推理；**"好不好看"这类主观体验，必须实测，不能用结构化推理替代**。这已经是本项目第二次栽在同一类错误上（上一次是 PvP 强度体感）。

---

## 6. 反复踩到的同一个坑

有一类 bug 我踩了**三次**，形态完全一样，值得单独列出来：

> **角色的 AI 站位距离 > 它自己的技能射程 = 它永远站在自己打不到的地方。**

| 角色 | 症状 | 原因 |
|---|---|---|
| 方里 | 胜率 5% | `kiteDist 120` > 技能射程 `85` |
| 江南 | 胜率 5% | `kiteDist 150` > 自身爆发半径 `85` |
| 江南（第二次） | 胜率 9% | 改成贴身型后血量不够，活不到放技能 |

第三次尤其讽刺：我为了削弱江南，把他血量从"高"砍到"中"，结果他直接掉到 9%——**贴身型角色没有血量就活不到释放技能**，削他应该削伤害。

**沉淀成检查项**：新增贴身型技能时，必须同时检查 `ai` / `kiteDist` / `range` 三者是否自洽；削贴身型角色**削伤害不削血量**。

---

## 7. 一次风险很低的大重构

单文件涨到 1223 行后拆成模块。做法值得记：

**不手抄，写脚本自动提取。** 用括号匹配解析器把 `ROSTER` 数组条目、`tickSkill` 的各个 `if(s.type===...)` 分支、英文翻译表按角色 id 归并，自动生成 25 个角色文件。

核心改动是把巨大的 if/else 链换成**技能注册表**，每个角色文件自带数据 + 机制实现：

```js
registerSkill('drain', { ai, init, cd, badges, tick, draw });
registerHero({ id, name, color, emblem, skill:{...}, i18n:{...} });
```

**如何确认重构没改变行为**：重构前后各跑一次完整强度榜。

```
重构前：极差 39pt
重构后：极差 40pt   ← 采样噪声范围内
```

这就是有测试台的好处：**重构的正确性不靠"我觉得没问题"，靠一个可量化的行为指纹。**

结果：`index.html` 约 210 行（只剩 HTML/CSS + script 标签）、`js/engine.js` ~1000 行、25 个角色文件、8 个地图文件、1 个图腾文件。加角色 = 加一个文件 + 一行 script，完全不碰引擎。

---

## 8. 可复用的方法论

抛开这个具体项目，下面几条是我认为可以迁移的：

1. **让自动化测试跑真实代码，不要跑简化模型。** 一旦分叉，你测的就是你的假设，不是系统。
2. **修底层 bug 前，先想清楚有没有上层参数在"补偿"它。** 那些参数看起来是调好的，不会自己报错。
3. **区分"指标"和"目标"。** 指标涨了不等于目标达成——先问这个指标测的是不是你要的东西。
4. **主观判断（好不好看、手感如何）交给实测，别用逻辑推理替代。**
5. **"数值怎么调都不对"时，问题通常在机制或几何层，不在数值层。** 纯接触流打不过护盾流，不是伤害不够，是它没有破盾的**手段**。
6. **记录你判断错的每一次。** 本文列的四次里，有三次的错误方向是一致的：**我过度信任了自己的结构化推理**。

---

## 9. 附：当前状态

| 项 | 数值 |
|---|---|
| 角色 / 机制 | 25 / 25（每个机制互不重复） |
| 地图 | 8 —— 矩形/圆形/菱形/十字，含障碍、伤害区、带电边界 |
| 1v1 平衡 | 极差 34pt，仅 2 个角色擦边出界 |
| 平均对局时长 | ~11s，超时率 0% |
| 冒烟测试 | 25 角色 × 8 地图 = 200/200 |
| 代码 | 36 个文件：1 HTML + 2 引擎 + 25 角色 + 8 地图；零依赖、零构建，双击即玩 |

**最复杂的一个技能**（《蛊真人》春秋蝉）作为收尾的例子：每 5 秒可发动，让**全场所有单位**回到 5→4→3→2→1 秒前（位置、血量、护盾全部倒流，**包括把这期间死掉的人复活**），五次用尽后永久失效；而施放者自己每秒 +5% 全属性，这份成长按**真实流逝时间**累积，**永不被自己的回溯抹掉**。

实测回溯幅度依次恢复 77 → 115 → 81 → 60 → **3** 点血——最后一次几乎没用了，机制自带上限。而他的属性倍率 1.25 → 2.25x 一路只涨不退。落点胜率 61%，**一次成型没有调整过数值**。

它之所以能一次做对，不是因为设计得多好，而是因为**做完立刻就能量化验证**。这大概是整个项目最大的收获。
